# 验收

## 1.3.2 已通过

- C# x64 WinForms 构建成功。
- `--self-test` 能识别包安装状态、官方 EXE、winget 和运行进程。
- `--update-self-test` 能读取公开版本清单、下载 Release 文件并核对 SHA-256。
- 拟态悬浮和克制玻璃两套主题可真实切换，关闭设置窗口后选择仍保存在 `settings.ini`。
- 主界面采用左侧状态与路径、右侧操作组；只有“一键恢复并启动”为亮蓝主操作。
- 设置页只显示两套主题与“检查版本”，不再暴露自动更新策略开关。
- EXE、桌面快捷方式、任务栏、主窗口和设置窗口均显示正式蓝色恢复图标。
- Windows DPI 感知已启用，主界面和设置页在当前高缩放桌面实际截图中无文字重叠或裁切。
- 小按钮无矩形底块和硬偏移阴影，圆角边界清楚。
- 单实例验证通过：先启动一个恢复中心，再启动第二个；第二个退出码为 0，进程数始终为 1。
- 自检能输出可用虚拟内存、提交压力和高压力判断；主界面在当前约 87% 提交压力下显示橙色资源提醒。
- 关于页包含初衷、恢复边界、隐私与本地数据说明。
- 日志使用独立主题窗口，避免主界面被技术信息挤满。
- 源码不包含 `Reset-AppxPackage` 或 `--ignore-certificate-errors`。
- 桌面快捷方式目标指向正式发布 EXE。

## 已有真实故障证据

- 2026-07-26 18:47:24 出现 AppModel `0x3CFC`。
- 18:47:51 恢复中心开始处理，注册后仍为 `Modified, NeedsRemediation`。
- 微软商店重新暂存约 179 秒，18:51:47 包状态从 `0x2` 恢复到 `0x0`。
- 18:51:48 安全启动成功，总耗时约 237 秒。

## 待后续真实事件回归

- 下一次 `NeedsRemediation` 故障确认注册等待约 12 秒、商店阶段实时计时正常。
- 下一版本发布时验证“下载新版 → SHA-256 → 用户确认 → 自替换 → 自动重启”完整链路。

当前活跃 Codex 会话中不主动制造故障进行破坏性测试。
