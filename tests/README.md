# 验收

2026-07-26 已通过：

- C# x64 WinForms 构建成功。
- `--self-test` 能识别包已安装、状态 `Ok`、官方 EXE 存在、winget 存在、Codex 正在运行。
- 桌面快捷方式目标指向发布 EXE。
- GUI 真实启动并自动写入状态日志。
- 源码不包含 `Reset-AppxPackage` 或 `--ignore-certificate-errors`。
- 1.1.0 x64 构建成功，GUI 真实启动并正确显示“现在状态正常”。
- 1.1.0 `--self-test` 通过：包状态 `Ok`、官方 EXE 与 winget 均存在。
- 主界面处理记录默认折叠，正常、异常、忙碌状态具有不同视觉反馈。
- 真实故障验收通过：18:47:24 出现 AppModel `0x3CFC`，18:51:47 包状态从 `0x2` 恢复到 `0x0`，18:51:48 成功启动。
- 1.2.0 待验证：下一次 `NeedsRemediation` 故障中确认注册等待约 12 秒、商店阶段实时计时正常。

当前活跃 Codex 会话中未主动再次执行修复，避免为测试而制造故障。
