# 更新记录

## 1.3.2 - 2026-07-26

- 增加全局单实例锁：重复双击只唤醒已有恢复中心，不再启动第二套恢复流程抢占 AppX 文件。
- 状态检查新增物理/虚拟内存余量与提交压力探针；高压力时在主界面直接提示。
- 自检报告新增可用虚拟内存、提交压力百分比和高压力判断，方便区分安装异常与资源压力。
- 根据 21:22 真实事件修复并发恢复缺陷：第二个实例曾因抢占 `AppxManifest.xml` 触发 `0x80070020`。

## 1.3.1 - 2026-07-26

- 为 EXE、桌面快捷方式、任务栏、主窗口和子窗口嵌入正式多尺寸恢复图标。
- 增加 Windows Per-Monitor DPI 感知，修复高缩放屏幕上文字发糊的问题。
- 移除小按钮的硬偏移阴影和矩形底块，改为干净圆角实体面；卡片改用轻边界层次。
- 修复主标题辅助文字和设置页说明在高 DPI 下重叠、裁切的问题。
- 设置页不再暴露“启动时检查”和“自动下载”开关，只保留“检查版本”；版本检查采用产品默认行为。

## 1.3.0 - 2026-07-26

- 依据窗口管理器、团建工作流与江湖转化助手的真实界面重新设计，不再沿用 1.1.0 的暖灰、墨绿临时方案。
- 默认采用冷灰蓝拟态悬浮：同色系实体卡片、明暗双阴影、左侧状态与恢复路径、右侧操作组，亮蓝仅用于一个主操作。
- 新增“拟态悬浮”和“克制玻璃”两套可切换主题，并持久保存选择。
- 新增完整设置页、版本检查和主题切换。
- 新增关于页与独立处理记录窗口，补齐产品初衷、恢复边界、隐私和数据位置说明。
- 新增公开版本清单与 SHA-256 校验更新链路；下载通过校验后仍需用户确认才会替换并重启。
- 更新检查改用 GitHub 最新 Release 携带的公共清单，避免 API 匿名限流和 Raw 分支缓存。
- 检查清单时附加防缓存参数，避免新版本刚发布后仍短暂读到旧清单。

## 1.2.0 - 2026-07-26

- 根据 18:47 的首次真实故障恢复记录优化耗时：`Modified, NeedsRemediation` 注册后等待由 45 秒缩短为 12 秒。
- 修正旧轮询会在截止时间后额外休眠的问题，改为基于真实计时、每 2 秒检查。
- 微软商店恢复阶段显示预计时间和实时已用秒数，每 15 秒记录一次进度，避免误以为程序卡死。
- 真实故障闭环已验证：`0x3CFC` → 注册修复 → 商店重新暂存 → 状态 `0x2` 恢复为 `0x0` → GPU 安全模式启动。

## 1.1.0 - 2026-07-26

- 首次图形界面重构。该版本采用暖灰与墨绿，后续确认不符合用户真实视觉系统，已由 1.3.0 全面替换。
- 状态、风险和下一步改为用户语言；处理记录默认折叠。
- 恢复算法和不清空应用数据的安全边界保持不变。

## 1.0.0 - 2026-07-26

- 首个可用版本。
- 增加图形化状态检查、安全启动和分级一键修复。
- 注册仍异常时，自动调用微软商店官方源重新暂存产品 `9PLM9XGG6VKS`。
- 不执行应用数据重置；日志只保存到本地运行数据目录。
